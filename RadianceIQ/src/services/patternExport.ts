import type { Pattern } from '../types';

// Stub — real implementation in Task 24 (Phase 9).
export async function exportAndSharePattern(_pattern: Pattern): Promise<void> {
  // No-op until react-native-view-shot is installed.
}
